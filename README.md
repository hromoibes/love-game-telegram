# 💞 Love4Two — Telegram Game Bot

Интерактивная игра для пар с искусственным интеллектом (Gemini).

## 🎮 Правила
- Отвечать можно: **«да», «нет», одно слово или медиа**
- У каждого игрока есть **1 пропуск** — `/skip`
- Есть **3 уровня пикантности** (1 — лёгкий флирт, 2 — средний, 3 — очень горячий)
- После `/finish` бот делает резюме и даёт советы 💌
- Вопросы по очереди, бот адаптируется к ответам и чаще повышает уровень после «да»
- На ответ 60 секунд: если молчите, бот пришлёт напоминание
- Без вопросов про бывших и анала

## ⚙️ Запуск
Хостинг: [Render.com](https://render.com)
Build: `pip install -r requirements.txt`
Start: `python bot.py`

## 🔑 Переменные окружения
- `TELEGRAM_TOKEN` — токен Telegram-бота
- `GEMINI_API_KEY` — API-ключ Google Gemini для генерации вопросов/резюме

## 🚀 Команды бота
- `/start` — сброс игры и ввод имён игроков
- `/rules` — показать правила
- `/question` — сгенерировать вопрос текущему игроку
- `/level [1|2|3]` — текущий уровень или установка вручную
- `/skip` — личный пропуск (1 на игрока)
- `/finish` — завершить игру и получить резюме

## 🧩 Как решать конфликт с ApplicationBuilder
Если в редакторе или на Render увидите конфликт вида:

```
<<<<<<< current change
ApplicationBuilder,
=======
main
>>>>>>> incoming change
```

Выбирайте вариант **с `ApplicationBuilder`** (или «Accept incoming change» в VS Code). 
В текущей версии бота инициализация идёт через `Application`/`ApplicationBuilder`, 
а импорт `main` — это ошибочный остаток старого мержа.
